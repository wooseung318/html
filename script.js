date.getFullYear();
date.getMonth() + 1;

currentYear % 4 === 0 && currentYear % 100 !== 0

Math.ceil((currentDay + currentLastDate)/7);

currentMonth - 1

currentYear + 1

<a href="#" class="button left" onclick="pcsCalendar(\''+id+'\',\''+prevDate+'\')">
<a href="#" class="button right" onclick="pcsCalendar(\''+id+'\',\''+prevDate+'\')">

calendar += '<tr>';

dateNum++

(dateNum < 1 || dateNum > currentLastDate)
dateString[j]
dateString[j]

pcsCalendar.innerHTML=calendar;
